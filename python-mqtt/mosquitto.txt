Broker for the metrics from the Pi:

Install the binaries for Mosquitto.
PS>.\mosquitto.exe -v -c mosquitto.conf

For testing if OpenCPN rower_pi is not used:

Subscriber for the rowdata:
PS C:\Program Files\mosquitto>  .\mosquitto_sub -d -h 192.168.1.165 -p 1883 -t 'OpenRowingMonitor/opencpn/metrics'

Show OpenRowingMonitor on pc:
http://192.168.1.215:1883